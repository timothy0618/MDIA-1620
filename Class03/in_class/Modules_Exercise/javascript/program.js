/**
 * @param department - The program department
 * @param degree - The name of the diploma
 * @returns  - I am studying the @degree in the @department department.
 * @desc exporting the function Program to be formatted in another file
 */

export var Program = (department, degree) => {
  return "I am studying the " + degree + " in the " + department + " department.";
};